for each s in states,
    Theorem Irrelevance-Env-for-ValueTyping-_(s): 
        forall*,
		(TypeOf : typeOf (nilEnvT) state_env(states) v t) -> 
		(ValueHyp : value v) ->  
		(_ : typeOf (nilEnvT) prime(state_env(states) only state_env(s)) v t). 
Proof.
if s in extensibleStates then doNotGenerateThisProof else 
	intros. 
	_ : case ValueHyp. 
	for each v in Value: _ : case TypeOf. search endfor
endif